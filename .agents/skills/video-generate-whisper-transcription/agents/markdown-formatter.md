# Markdown Formatter

Validate the Markdown as a knowledge-base artifact.

- Confirm frontmatter exists and is UTF-8 readable.
- Confirm sections use time ranges and paragraphs are readable.
- Avoid over-formatting; the Markdown should be useful for future RAG ingestion.
- Keep timestamps as section anchors, not as a timestamp on every sentence.
