# Technical Validator

Check technical completion.

- Confirm expected native files exist when emitted by the backend.
- Confirm `<videoname>.md` and `text_transcription_report.json` exist for each successful video.
- Confirm batch failures are reported without hiding successful outputs.
- Run or request relevant tests after script changes.
